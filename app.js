const form = document.querySelector("form");

form.addEventListener("submit", (e) => {
  e.preventDefault();
  let firstName = "";
  let lastName = "";
  let email = "";
  let password = "";
});

function validation(data) {
  console.log(data);
}
